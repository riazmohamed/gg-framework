[package]
name = "gg-app"
version = "0.62.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "gg_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# `tray-icon` + `image-png` are NOT default tauri features: the first adds the
# macOS menu-bar status item, the second lets `Image::from_bytes` decode the PNG
# tray mark (icons/tray-mac.png).
tauri = { version = "2", features = [
  "macos-private-api",
  "tray-icon",
  "image-png",
] }
tauri-plugin-opener = "2"
tauri-plugin-log = "2"
tauri-plugin-dialog = "2"
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# `query` became an opt-in feature in 0.13. We ask for no TLS feature of our
# own — every request goes to the local sidecar over plain http://127.0.0.1 —
# but tauri-plugin-updater turns on reqwest's `rustls-no-provider`, and cargo
# unifies features across the shared 0.13 build. So OUR client is a rustls
# client too, and building one panics unless a crypto provider is installed
# first. See `install_rustls_provider` in lib.rs.
reqwest = { version = "0.13", default-features = false, features = ["json", "query", "stream"] }
# Only to install that provider. `ring` is the same backend (and version) the
# updater already pulls in, so this adds no crates to the build.
rustls = { version = "0.23", default-features = false, features = ["ring"] }
futures-util = "0.3"
tokio = { version = "1", features = ["time"] }
base64 = "0.23"
uuid = { version = "1", features = ["v4"] }

# Updater + process (relaunch) are desktop-only — the updater plugin doesn't
# build for mobile targets.
[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-updater = "2"
tauri-plugin-process = "2"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

